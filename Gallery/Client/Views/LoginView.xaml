<UserControl x:Class="Client.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox Width="200" Margin="5" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
            <PasswordBox Width="200" Margin="5" PasswordChanged="PasswordBox_PasswordChanged"/>
            <Button Width="200" Margin="5" Content="Login" Command="{Binding LoginCommand}"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
