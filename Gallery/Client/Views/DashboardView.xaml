<UserControl x:Class="Client.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Edit User Button -->
        <Button Grid.Row="0" Grid.Column="1" FontSize="16" Width="100" Margin="5" Content="View User" Command="{Binding EditUserCommand}" HorizontalAlignment="Left" />

        <TextBox Grid.Row="0" Grid.Column="1" Width="200" FontSize="16" Margin="5 5 10 5" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" HorizontalContentAlignment="Center"  VerticalContentAlignment="Center"/>
        <Button Grid.Row="0" Grid.Column="1" FontSize="16" Width="100" Margin="20, 5, 120, 5" Content="Search" Command="{Binding SearchCommand}" HorizontalAlignment="Right" />
        <Button Grid.Row="0" Grid.Column="2" FontSize="16" Width="100" Margin="5" Content="Logout" Command="{Binding LogoutCommand}" HorizontalAlignment="Right" />
        <TextBox Grid.Row="0" Grid.Column="0" Text="{Binding LoggedInUsername}" Margin="5" Padding="5" FontSize="16"  IsReadOnly="True"/>

        <ListView Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" FontSize="18" ItemsSource="{Binding Galleries}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="PIB" DisplayMemberBinding="{Binding Pib}" />
                    <GridViewColumn Header="Address" DisplayMemberBinding="{Binding Address}" />
                    <GridViewColumn Header="MBR" DisplayMemberBinding="{Binding Mbr}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
