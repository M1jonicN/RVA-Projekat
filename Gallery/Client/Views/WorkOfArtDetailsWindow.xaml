<Window x:Class="Client.Views.WorkOfArtDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Work of Art Details" Height="600" Width="450" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="10">
            <TextBlock Text="Logged in as: " FontSize="14" FontWeight="Bold"/>
            <TextBlock Text="{Binding LoggedInUsername}" FontSize="14" Margin="5,0,0,0"/>
        </StackPanel>
        <StackPanel Orientation="Vertical" Grid.Row="1" Margin="10">
            <TextBlock Text="Work of Art Details" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="Art Name"/>
            <TextBox Text="{Binding WorkOfArt.ArtName, Mode=TwoWay}" FontSize="16" IsEnabled="{Binding IsWorkOfArtEditing}"/>
            <TextBlock Text="Art Movement"/>
            <ComboBox ItemsSource="{Binding ArtMovements}" SelectedItem="{Binding WorkOfArt.ArtMovement, Mode=TwoWay}" FontSize="16" IsEnabled="{Binding IsWorkOfArtEditing}"/>
            <TextBlock Text="Style"/>
            <ComboBox ItemsSource="{Binding Styles}" SelectedItem="{Binding WorkOfArt.Style, Mode=TwoWay}" FontSize="16" IsEnabled="{Binding IsWorkOfArtEditing}"/>
            <TextBlock Text="Gallery PIB"/>
            <TextBox Text="{Binding WorkOfArt.GalleryPIB, Mode=TwoWay}" FontSize="16" IsEnabled="False"/>
            <StackPanel Orientation="Horizontal" Margin="0,10" HorizontalAlignment="Center">
                <Button Margin="0,0,100,0" Content="Edit" Command="{Binding EditWorkOfArtCommand}" Visibility="Visible"/>
                <Button Content="Save" Command="{Binding SaveWorkOfArtCommand}" Visibility="{Binding IsWorkOfArtEditing, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=true}"/>
            </StackPanel>
        </StackPanel>
        <StackPanel Orientation="Vertical" Grid.Row="2" Margin="10,-20,10,0">
            <TextBlock Text="Author Details" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="First Name"/>
            <TextBox Text="{Binding Author.FirstName, Mode=TwoWay}" FontSize="16" IsEnabled="{Binding IsAuthorEditing}"/>
            <TextBlock Text="Last Name"/>
            <TextBox Text="{Binding Author.LastName, Mode=TwoWay}" FontSize="16" IsEnabled="{Binding IsAuthorEditing}"/>
            <TextBlock Text="Birth Year"/>
            <TextBox Text="{Binding Author.BirthYear, Mode=TwoWay}" FontSize="16" IsEnabled="{Binding IsAuthorEditing}"/>
            <TextBlock Text="Death Year"/>
            <TextBox Text="{Binding Author.DeathYear, Mode=TwoWay}" FontSize="16" IsEnabled="{Binding IsAuthorEditing}"/>
            <TextBlock Text="Art Movement"/>
            <ComboBox ItemsSource="{Binding ArtMovements}" SelectedItem="{Binding Author.ArtMovement, Mode=TwoWay}" FontSize="16" IsEnabled="{Binding IsAuthorEditing}"/>
            <StackPanel Orientation="Horizontal" Margin="0,10" HorizontalAlignment="Center">
                <Button Margin="0,0,40,0" Content="Edit" Command="{Binding EditAuthorCommand}" Visibility="Visible"/>
                <Button Margin="0,0,40,0" Content="Save" Command="{Binding SaveAuthorCommand}" Visibility="{Binding IsAuthorEditing, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=true}"/>
                <Button Content="Delete" Command="{Binding DeleteAuthorCommand}" Visibility="{Binding IsAuthorEditing, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=true}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
